MODULE main
	VAR
		line: 1..3;
		x: 0..3;
		y: 0..3;
	ASSIGN

	-- init(line) := 1;
	init(line) := 1;
	next(line) := 2;
		-- case
		-- 	(line=1) : 2; 
		-- 	(line=2) : {3}; 
		-- 	TRUE: line;
		-- esac;

	init(x) := 3;
	next(x) := 3;

	init(y) := 3;
	next(y) := (x mod (y-1));


	DEFINE
		obs_print := (line=2) | (line=3);
		halt := (line=3);